name: Release Build

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: macos-latest
    permissions:
      contents: write

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'

    - name: Install dependencies
      run: npm ci

    - name: Build & Publish
      run: npx electron-builder --mac --arm64 --publish=always
      env:
        # GITHUB_TOKEN pour créer la release
        GH_TOKEN: ${{ secrets.GH_PAT_UPDATER }}
        # Token embarqué dans app-update.yml pour que l'app télécharge les mises à jour
        EP_GITHUB_TOKEN: ${{ secrets.GH_PAT_UPDATER }}
        CSC_IDENTITY_AUTO_DISCOVERY: false
